const express = require('express');

/**
 * TODO:
 * Bring in body parser (json) and use as middleware (app.use).
 * Set up a basic route (app.get)
 */

const app = express();



module.exports = app;